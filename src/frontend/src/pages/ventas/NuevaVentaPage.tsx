/**
 * HU-01 — Nueva Venta.
 *
 * ⚠️ PLACEHOLDER: este componente se implementará completamente en el Paso 5.
 */
export function NuevaVentaPage() {
  return (
    <main className="min-h-screen bg-slate-50 p-6">
      <header className="mb-6">
        <h1 className="text-2xl font-bold text-slate-800">Nueva Venta</h1>
        <p className="text-sm text-slate-500">
          HU-01 — Pantalla de punto de venta con validación de stock en tiempo real.
        </p>
      </header>

      <div className="rounded-lg border border-dashed border-slate-300 bg-white p-12 text-center">
        <p className="text-slate-400">
          🔨 Implementación en progreso (Pasos 3, 4 y 5 del scaffold)...
        </p>
      </div>
    </main>
  );
}
